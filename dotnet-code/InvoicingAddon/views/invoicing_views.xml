<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="account.view_move_tree" model="ir.ui.view">
            <field name="name">account.move.tree</field>
            <field name="model">account.move</field>
            <field name="arch" type="xml">
                <tree string="Invoices &amp; Bills">
                    <field name="id"/>
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="invoice_type"/>
                    <field name="amount_total"/>
                    <field name="status"/>
                </tree>
            </field>
        </record>

        <record id="account.view_move_line_tree" model="ir.ui.view">
            <field name="name">account.move.line.tree</field>
            <field name="model">account.move.line</field>
            <field name="arch" type="xml">
                <tree string="Invoice Lines">
                    <field name="product_id"/>
                    <field name="lot_id"/>
                    <field name="quantity"/>
                    <field name="price_unit"/>
                    <field name="price_subtotal"/>
                </tree>
            </field>
        </record>

        <record id="account.report_move" model="ir.ui.view">
            <field name="name">account.move.report</field>
            <field name="model">account.move</field>
            <field name="arch" type="xml">
                <t t-name="account.move.report">
                    <html>
                        <head>
                            <style>
                                body { font-family: sans-serif; padding: 2rem; color: #222; }
                                table { width: 100%; border-collapse: collapse; margin-top: 1rem; }
                                th, td { border: 1px solid #ccc; padding: 6px 10px; text-align: left; }
                                th { background: #f5f5f5; }
                                .total { text-align: right; margin-top: 1rem; font-size: 1.2rem; }
                            </style>
                        </head>
                        <body>
                            <h2 t-esc="record.name"/>
                            <p>Bill To: <span t-esc="record.partner_id"/></p>
                            <p>Status: <span t-esc="record.status"/></p>
                            <table>
                                <thead>
                                    <tr><th>Description</th><th>Qty</th><th>Unit Price</th><th>Subtotal</th></tr>
                                </thead>
                                <tbody>
                                    <tr t-foreach="record.invoice_line_ids" t-as="line">
                                        <td t-esc="line.name"/>
                                        <td t-esc="line.quantity"/>
                                        <td t-esc="line.price_unit"/>
                                        <td t-esc="line.price_subtotal"/>
                                    </tr>
                                </tbody>
                            </table>
                            <div class="total">Total: <span t-esc="record.amount_total"/></div>
                        </body>
                    </html>
                </t>
            </field>
        </record>

        <record id="account.view_move_form" model="ir.ui.view">
            <field name="name">account.move.form</field>
            <field name="model">account.move</field>
            <field name="arch" type="xml">
                <form string="Invoice">
                    <header>
                        <button name="ActionPost" string="Post" type="object" class="btn-success" invisible="status != 'draft'"/>
                        <button string="Print" type="report" template="account.report_move" class="btn-outline-secondary"/>
                    </header>
                    <sheet>
                        <group string="Details">
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="invoice_type"/>
                            <field name="invoice_line_ids"/>
                            <field name="amount_total"/>
                            <field name="status"/>
                        </group>
                    </sheet>
                    <div class="oe_chatter"/>
                </form>
            </field>
        </record>

        <menuitem id="invoicing_app" name="Invoicing" icon="bi-receipt" action_type="act_window" model="account.move" view_mode="tree,kanban,form"/>

        <record id="demo_inv_1" model="account.move">
            <field name="name">INV/2026/001</field>
            <field name="partner_id">1</field>
            <field name="invoice_type">out_invoice</field>
            <field name="amount_total">1200.0</field>
            <field name="status">posted</field>
        </record>
    </data>
</odoo>
